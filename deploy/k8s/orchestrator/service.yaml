apiVersion: v1
kind: Service
metadata:
  name: dotnet-diagnostics-orchestrator
  namespace: diagnosticsmcp-system
  labels:
    app.kubernetes.io/name: dotnet-diagnostics-orchestrator
    app.kubernetes.io/component: orchestrator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dotnet-diagnostics-orchestrator
  ports:
    - name: http
      port: 5130
      targetPort: http
# Optional authenticated ingress example. Do NOT expose the orchestrator directly to the
# public internet; put an auth proxy / mTLS boundary in front of it.
# ---
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dotnet-diagnostics-orchestrator
#   namespace: diagnosticsmcp-system
# spec:
#   ingressClassName: nginx
#   rules:
#     - host: diagnostics.example.internal
#       http:
#         paths:
#           - path: /
#             pathType: Prefix
#             backend:
#               service:
#                 name: dotnet-diagnostics-orchestrator
#                 port:
#                   number: 5130
